# {{.Name}}

{{.Description}}

## Usage

```yaml
- uses: {{gitUsesString .}}
  {{if .Inputs}}with:
  {{- range $key, $val := .Inputs}}
    {{$key}}: {{if $val.Default}}{{$val.Default}}{{else}}value{{end}}
  {{- end}}{{end}}
```

{{- if .Inputs}}

## Inputs
{{range $key, $input := .Inputs}}
- `{{$key}}` - {{$input.Description}}{{if $input.Required}} (required){{end}}{{if hasDefault $input.Default}} (default: {{$input.Default | mdCode}}){{end}}
{{- end}}
{{- end}}

{{- if .Outputs}}

## Outputs
{{range $key, $output := .Outputs}}
- `{{$key}}` - {{$output.Description}}
{{- end}}
{{- end}}

{{- if and .Permissions (gt (len .Permissions) 0)}}

## Permissions
{{range $key, $value := .Permissions}}
- `{{$key}}`: `{{$value}}`
{{- end}}
{{- end}}

{{- if .License}}

## License

{{.License}}
{{- end}}
