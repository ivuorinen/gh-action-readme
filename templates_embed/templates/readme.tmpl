# {{.Name}}

> {{.Description}}

## Usage

```yaml
- uses: {{gitUsesString .}}
  with:
{{- range $key, $val := .Inputs}}
    {{$key}}: # {{$val.Description}}{{if hasDefault $val.Default}} (default: {{$val.Default}}){{end}}
{{- end}}
```

## Inputs

{{range $key, $input := .Inputs}}
- **{{$key}}**: {{$input.Description}}{{if $input.Required}} (**required**){{end}}{{if hasDefault $input.Default}} (default: {{$input.Default}}){{end}}
{{end}}

{{if .Outputs}}
## Outputs

{{range $key, $output := .Outputs}}
- **{{$key}}**: {{$output.Description}}
{{end}}
{{end}}

{{if and .Permissions (gt (len .Permissions) 0)}}
## Permissions

This action requires the following permissions:

{{range $key, $value := .Permissions}}
- **`{{$key}}`**: `{{$value}}`
{{end}}

**Usage in workflow:**
```yaml
permissions:
{{- range $key, $value := .Permissions}}
  {{$key}}: {{$value}}
{{- end}}
```
{{end}}

## Example

See the [action.yml](./action.yml) for a full reference.

---

*Auto-generated by [gh-action-readme](https://github.com/ivuorinen/gh-action-readme)*
